# Demo only: deliberately discard previous-CPU affinity within the LLC.
fallback = "any_allowed"

[[rung]]
operation = "pick_random_idle"
scope = "previous_llc"

[[rung]]
operation = "pick_random_idle"
scope = "task_allowed"
