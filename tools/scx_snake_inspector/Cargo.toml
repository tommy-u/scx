[package]
name = "scx_snake_inspector"
version = "0.1.0"
edition = "2021"
license = "GPL-2.0-only"
description = "Local scheduler inspector for scx_snake"

[workspace]

[dependencies]
anyhow = "1"
axum = "0.8"
clap = { version = "4", features = ["derive"] }
getrandom = "0.4"
libbpf-rs = "=0.26.2"
libc = "0.2"
scx_utils = { path = "../../rust/scx_utils", version = "1.1.1" }
scx_stats = { path = "../../rust/scx_stats", version = "1.1.1" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tokio = { version = "1", features = ["macros", "net", "process", "rt-multi-thread", "signal", "sync", "time"] }
tokio-stream = { version = "0.1", features = ["sync"] }
tempfile = "3"

[dev-dependencies]
http-body-util = "0.1"
tower = { version = "0.5", features = ["util"] }

[build-dependencies]
scx_cargo = { path = "../../rust/scx_cargo", version = "1.1.1" }
